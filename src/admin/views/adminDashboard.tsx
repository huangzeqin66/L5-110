import React from 'react';

const AdminDashboard: React.FC = () => {
    return (
        <div>
            <h1>管理员仪表板</h1>
            <p>在这里管理预约。</p>
            {/* 这里可以添加更多的组件和功能，例如预约列表、创建预约表单等 */}
        </div>
    );
};

export default AdminDashboard;